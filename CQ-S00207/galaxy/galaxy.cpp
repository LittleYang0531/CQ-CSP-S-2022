#include <bits/stdc++.h>
using namespace std;
int main() {
	freopen("galaxy.in", "r", stdin);
	freopen("galaxy.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	for (int i = 1, x;i <= m * 2;i++) {
		cin >> x;
	}
	int q;
	cin >> q;
	while(q--) {
		printf("NO\n");
	}
	return 0;
}
