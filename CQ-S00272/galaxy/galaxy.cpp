#include<bits/stdc++.h>
using namespace std;

int main() {
	freopen("galaxy.in", "r", stdin);
	freopen("galaxy.out", "w", stdout);
	int n, m, q;
	cin >> n >> m;
	for(int i = 1; i <= m; i++) cin >> u>> v;
	cin >> q;
	while(q--) cout << "NO" << endl;
}
