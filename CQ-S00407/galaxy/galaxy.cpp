#include <bits/stdc++.h>
using namespace std;
int n, m;

int main() {
    freopen("galaxy.in", "r", stdin);
    freopen("galaxy.out", "w", stdout);
	scanf("%d%d", &n, &m);
	while (m --) {
		int u, v;
		scanf("%d%d", &u, &v);
	}
	int q;
	scanf("%d", &q);
	for (int i = 1; i <= q; i ++) cout << "NO" << endl;

	return 0;
}

